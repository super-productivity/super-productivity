<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
  <cloud-backup>
    <!-- Prevent restored legacy launch_mode from pinning fresh installs to online mode. -->
    <exclude
      domain="sharedpref"
      path="com.superproductivity.superproductivity_preferences.xml" />
    <!-- Widget IDs are not stable across restore/transfer; do not transfer selections or pending navigation. -->
    <exclude
      domain="sharedpref"
      path="task_list_widget.xml" />
  </cloud-backup>
  <device-transfer>
    <!-- A transferred fresh install should select launch mode from current state. -->
    <exclude
      domain="sharedpref"
      path="com.superproductivity.superproductivity_preferences.xml" />
    <!-- Widget IDs are not stable across restore/transfer; do not transfer selections or pending navigation. -->
    <exclude
      domain="sharedpref"
      path="task_list_widget.xml" />
  </device-transfer>
</data-extraction-rules>
